version: '3.8'

services:
  arangodb:
    image: arangodb:latest
    container_name: arango-projekt
    environment:
      - ARANGO_ROOT_PASSWORD=root
    ports:
      - "8529:8529"
    volumes:
      - arango_data:/var/lib/arangodb3

volumes:
  arango_data: